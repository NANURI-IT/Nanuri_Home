# 2026-04-13 다크 3D 리디자인 마감 스펙

## Context
초안 다크 3D 리디자인 완료 후 발견된 개선 사항 21개.
High / Medium / Low 우선순위로 나눠 일괄 처리.

**완료일**: 2026-04-13

---

## ✅ High Priority (기본 품질)

- [x] **1. 라이트 모드 텍스트 가독성**
  - `text-white/*` 하드코딩 → `text-ink`, `text-body`, `text-dim` 전환
  - 13개 파일 처리 완료

- [x] **2. 3D 성능 — 백그라운드 탭 정지**
  - `document.visibilitychange` 핸들링
  - R3F `frameloop` 동적 전환 + MeshGradient rAF 일시정지

- [x] **3. 폰트 전역 적용**
  - globals.css에 `h1, h2, h3` Outfit 자동 적용
  - `.mono` 유틸 클래스

- [x] **4. OG 이미지 다크 3D 팔레트**
  - `opengraph-image.tsx` — 다크 3D 팔레트로 전면 교체
  - 시안→인디고 그라디언트 + 글로우 orb

- [x] **5. 서비스 페이지 이미지 래퍼**
  - `bg-white rounded-xl p-4` 래퍼 추가
  - IbimsFeatures/IbimsWorkflow/AcctWorkflow/ChannelFunctions

---

## 🟡 Medium Priority (마감 품질)

- [x] **6. Metrics 섹션 Hero 오버랩**
  - `-mt-16` 오버랩, 4칸 글래스 그리드
  - 시안 카운터 + Space Mono 라벨

- [x] **7. CoreSolution IBIMS floating-card**
  - Deal Pipeline +247% (cyan) / 운용효율 98.7% (indigo) / 시스템 안정성 A++ (emerald)
  - progress bar + float-y 애니메이션

- [x] **8. 기술 마퀴 섹션 추가**
  - `TechMarquee.tsx` 14개 기술 항목
  - 35s linear infinite + 좌우 fade mask
  - TrustFactors와 ContactCTA 사이 배치

- [x] **9. ContactCTA conic-gradient**
  - `.conic-rotate` 배경 적용
  - 시안→인디고 그라디언트 CTA + 전화번호 ghost 버튼

- [x] **10. BusinessAreas 5색 차별화**
  - IB(indigo) · 고유자산(emerald) · STO(rose) · 회계(amber) · 채널(cyan) · 퇴직연금(indigo)
  - 아이콘 박스 + blur glow
  - 영문 태그(Deal Sourcing · Performance 등) hover fadeIn

- [x] **11. History 타임라인 시안 + 교대**
  - 수직 라인 cyan→indigo 그라디언트
  - 연도 배지 시안→인디고 그라디언트 + 시안 글로우
  - dot marker 시안 + 글로우

- [x] **12. react-xarrows 다이어그램 색상**
  - 이전 단계에서 이미지로 교체 → 영향 없음

- [x] **13. Services 태그라인 hover**
  - 10번과 통합 처리 (BusinessAreas)

---

## 🟢 Low Priority (디테일)

- [x] **14. ThemeToggle 접근성**
  - focus-visible ring (시안 2px)

- [x] **15. 드롭다운 터치 대응**
  - `onClick` 토글 + outside click close
  - mousedown/touchstart 양쪽 리스너

- [x] **16. 노이즈 오버레이 강도**
  - 0.035 → 0.025로 감소

- [x] **17. prefers-reduced-motion 연동**
  - Three.js frameloop → `never`
  - MeshGradient rAF 일시정지
  - CSS 애니메이션 비활성

- [x] **18. SEO 메타데이터 일관성**
  - 모든 페이지에 metadata export 확인
  - layout template `"%s | 나누리아이티"` 자동 합성

- [x] **19. 서비스 영문 배지 일관성**
  - BusinessAreas에서 English 필드 정리 완료

- [x] **20. 모바일 햄버거 메뉴 글래스**
  - `rgba(4,4,12,0.92)` + `blur(30px) saturate(1.6)`

- [x] **21. 헤더 드롭다운 투명도 강화 (사용자 피드백)**
  - 기존: 옅은 glass → 배경 묻힘
  - 수정: `rgba(4,4,12,0.92)` + `blur(40px) saturate(1.8)` + 시안 글로우 shadow
  - 가독성 확보

---

## 🏗 최종 구현 내역

**신규 파일**: `src/components/TechMarquee.tsx`
**수정 파일 (20+개)**:
- globals.css (h1/h2/h3 Outfit, float-card/bar-grow 애니메이션, 노이즈 강도)
- layout.tsx (폰트 확장)
- page.tsx (TechMarquee 삽입, 섹션 재배치)
- Header.tsx (드롭다운 터치/클릭/투명도 강화)
- ThemeToggle.tsx (focus-visible)
- SceneContainer.tsx + MeshGradient.tsx (visibilitychange + reduced-motion)
- opengraph-image.tsx (다크 3D 팔레트 재생성)
- StatsCounter.tsx (Hero 오버랩 재설계)
- CoreSolution.tsx (floating-card 3개 + feature-list)
- BusinessAreas.tsx (5색 차별화 + 태그라인)
- ContactCTA.tsx (conic-gradient 배경)
- about/History.tsx (시안 타임라인)
- 기타 서비스/컴포넌트 text-white/* 정리

## 검증
- ✅ `npm run build` 통과
- ✅ 13 routes 정적 생성 성공
- ✅ TypeScript 에러 없음
